{
  "name": "webview-bug-demo",
  "version": "0.0.1",
  "description": "Demonstrates webview bug when webviews left attached to the DOM make app remain a background process after exit",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-app": "electron-packager . webview-bug-demo --platform=win32 --arch=x64 --overwrite --asar"
  },
  "author": "maxos",
  "license": "ISC",
  "dependencies": {
    "electron-tabs": "^0.9.2"
  },
  "devDependencies": {
    "electron": "^2.0.6"
  }
}
